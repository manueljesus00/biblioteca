# Dockerfile para desarrollo del Backend (con hot reload)

FROM node:20-alpine

WORKDIR /app

# Copiar archivos de dependencias
COPY package*.json ./

# Instalar dependencias
RUN npm install

# Copiar prisma
COPY prisma ./prisma/

# Generar Prisma Client
RUN npx prisma generate

# Exponer el puerto del backend
EXPOSE 3000

# El código se montará como volumen, no se copia
# El comando se ejecuta desde docker-compose
CMD ["npm", "run", "dev"]
